from setuptools import setup, find_packages

setup(
    name="dynamic-cookie-scanner",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here or reference requirements.txt
    ],
    python_requires=">=3.8",
)
